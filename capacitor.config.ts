import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angelxehg.studylater',
  appName: 'StudyLater',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
